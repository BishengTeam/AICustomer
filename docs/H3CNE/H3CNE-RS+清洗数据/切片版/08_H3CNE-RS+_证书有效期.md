# H3CNE-RS+ 证书有效期
问：H3CNE-RS+ 证书有效期多久？  
答：根据当前资料，H3CNE-RS+ 证书有效期为 `3 年`。

问：H3CNE-RS+ 证书几年过期？  
答：当前资料支持的口径是 `3 年`。

问：用户只问“有效期几年”时要不要直接展开重认证？  
答：不建议。用户只问有效期时，应优先直接回答“证书有效期为 3 年”；只有用户继续追问“到期后怎么办”，再结合重认证规则回答。

---

## 使用建议

由于有效期属于高风险字段，正式回答时建议使用以下表述：

“根据当前资料，H3CNE-RS+ 证书有效期为 3 年，最新规则建议以官方公告为准。”

同时注意：

- 如果用户只问“几年”，不要直接把回答重心放到重认证
- 如果用户继续问“到期后怎么办”，再结合 `09_H3CNE-RS+_重认证说明.md` 回答
